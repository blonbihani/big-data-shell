# big-data-shell
Shell commands for processing data

## Bash

Helpful commands
```Bash

ls
cat r.txt > a.txt
cat r.txt >> a.txt
mkdir src
touch .gitignore
curl " url" > a.txt
```

## PowerShell

Helpful commands

``` PowerShell

```
